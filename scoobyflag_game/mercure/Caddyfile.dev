# Learn how to configure the Mercure.rocks Hub on https://mercure.rocks/docs/hub/config
{
<<<<<<< HEAD
    auto_https off
}

localhost:3000
=======
	{$GLOBAL_OPTIONS}
}

{$SERVER_NAME:localhost}

log {
	format filter {
		wrap console
		fields {
			uri query {
				replace authorization REDACTED
			}
		}
	}
}

{$EXTRA_DIRECTIVES}
>>>>>>> gaetanDev

route {
	encode zstd gzip

	mercure {
		# Transport to use (default to Bolt)
		transport_url {$MERCURE_TRANSPORT_URL:bolt://mercure.db}
		# Publisher JWT key
		publisher_jwt !ChangeThisMercureHubJWTSecretKey! {env.MERCURE_PUBLISHER_JWT_ALG}
		# Subscriber JWT key
		subscriber_jwt !ChangeThisMercureHubJWTSecretKey! {env.MERCURE_SUBSCRIBER_JWT_ALG}
		# Permissive configuration for the development environment
		cors_origins *
		publish_origins *
		demo
		anonymous
		subscriptions
		# Extra directives
		{$MERCURE_EXTRA_DIRECTIVES}
	}

	redir / /.well-known/mercure/ui/
	respond /healthz 200
	respond "Not Found" 404
}
